services:
  - type: worker
    name: telegram-link-collector
    runtime: docker
    dockerfilePath: Dockerfile
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: DATABASE_PATH
        value: data/database.db
